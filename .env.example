# ==============================================
# Minecraft Server 設定
# ==============================================

# メモリ割り当て（JVMヒープサイズ）
# 目安: MOD数50個程度なら 4G、100個以上なら 6〜8G
# PCの搭載メモリの半分以下に設定してください
MC_MEMORY=16G

# サーバーポート（通常は変更不要）
MC_PORT=25565

# RCON パスワード（外部に公開しない場合はデフォルトでもOK）
RCON_PASSWORD=changeme

# ==============================================
# Tailscale 設定（任意）
# 設定しなければ Tailscale は無効のまま動作します。
# localhost からの接続は Tailscale の有無に関係なく可能です。
# ==============================================

# Tailscale Auth Key
# Tailscale 管理画面 (https://login.tailscale.com/admin/settings/keys)
# で生成した Auth Key をここに設定してください。
# タグ付き (tag:mc-server) の Auth Key を推奨します。
# 空欄のままならば Tailscale は未認証のまま起動します（通常動作に影響なし）。
TS_AUTHKEY=tskey-auth-XXXXXXXXXXXXXXXX-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Tailscale の追加引数（通常は変更不要）
# タグやデバイス名を変更したい場合はここを編集してください。
# --hostname=... がTailnet上でのデバイス名になります。
TS_EXTRA_ARGS=--advertise-tags=tag:mc-server --hostname=mc-server
